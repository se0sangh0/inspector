# 괴이탐사국 프로젝트 작업 규칙

이 문서는 이 저장소에서 함께 작업하는 사람과 자동화 작업자가 공통으로 지킬
프로젝트 규칙을 정의한다. 특정 작업자, 계정 또는 호출 명령은 프로젝트 규칙으로
고정하지 않는다. 도구 선택은 사용자의 현재 지시와 각 실행
환경의 안전 규칙을 따른다.

사용자의 현재 지시가 이 문서보다 우선한다. 하위 디렉터리에 더 가까운
`AGENTS.md` 또는 `AGENTS.override.md`가 있으면 그 범위에서는 해당 문서를 우선한다.

## 1. 기준 문서와 우선순위

1. 현재 대화에서 받은 사용자 지시
2. Notion의 [괴이탐사국 팀 기획서](https://app.notion.com/p/3af6aef5baac800298b6c2154b9b2699)와 하위 `기획 문서`
3. Notion의 [15. 미결 안건표](https://app.notion.com/p/3af6aef5baac8185866ff29253c2e69f)
4. 현재 체크아웃의 코드·씬·프리팹과 직접 확인한 테스트 결과
5. `HANDOFF.md`의 구현 이력과 알려진 함정

- `archive/2026-08-01_Notion_이전/` 아래의 `기획/`, `기획_통합/`,
  `디자인_UI/`은 읽기 전용 아카이브다. Notion에 접근할 수 없을 때
  아카이브를 최신 기획으로 간주하지 않는다.
- 기획과 구현이 충돌하면 먼저 Notion의 확정·미결 상태를 확인한다. 미결 안건을
  코드나 다른 문서가 임의로 확정해서는 안 된다.
- 시스템 기획 문장은 명시적 주어와 한 가지 지시로 한 기능만 설명한다.
- 팀 공통 용어는 Notion `용어 사전`, 디자인 기준과 생성 이력은 Notion
  `디자인 가이드`와 `AI 디자인 생성 기록`을 따른다.

## 2. 협업 방식

- 작업 시작 전에 현재 요청과 기준 문서에서 목표, 수정 범위와 완료 조건을 파악한다.
  이미 정해진 사항은 다시 묻지 않는다.
- 분석·검토 요청은 읽기 전용으로 수행한다. 변경 요청은 승인된 범위의 구현과
  검증까지 마친다. 통상적인 구현 선택은 기존 규칙과 코드에 맞춰 판단한다.
- 결과나 승인 범위에 영향을 주는 미결 사항만 질문한다. 답변이 필요한 부분은
  보류하되, 답변과 무관하게 진행할 수 있는 승인된 작업은 계속한다.
- 같은 파일을 여러 작업자가 동시에 수정하지 않는다. 병렬 작업은 파일 또는
  책임 범위가 겹치지 않을 때만 진행한다.
- 작업자는 자신에게 배정되지 않은 변경을 되돌리거나 정리하지 않는다.
- 새로운 기획 결정은 관련 원문에 반영하고 `15. 미결 안건표`의 활성 안건 또는
  완료 기록에도 같은 날 남긴다.
- 현재 범위를 넘어서는 기능, 아키텍처 변경, 대규모 데이터 이전이 필요하면
  승인된 범위에서 영향 분석과 검토 가능한 변경안을 준비한 뒤 승인받는다.
- 문서 변경은 한 변경에서 한 목적만 다룬다. 구조 이동이나 이름 변경이 있으면
  허브, 목차, 상대 링크와 참조 문서를 함께 점검한다.
- 상세 Git·회의·파일 이름 규칙은 `운영/00_협업_운영_규칙.md`를 단일 기준으로
  사용한다.

### AI 보조 검토 원칙

- AI 보조 검토의 추론 수준은 작업의 복잡도와 변경 위험에 맞춰 선택한다.
- 긴 문서 전체와 서로 독립적인 검토 축을 한 번에 처리하지 않는다. 문서·규칙·실패
  분기 단위로 나누고, 완료된 부분 결과만 별도로 통합한다.
- 한도 도달이나 호출 실패를 완료된 검토로 간주하지 않는다. 실패 원인, 완료된 범위,
  검토하지 못한 범위를 보고하고 입력 범위를 줄여 다시 검토한다.
- 지원되지 않는 설정이나 실행 경로를 임의로 대체하지 않는다. 특정 모델명, 호출
  매개변수와 비용 정책은 실행 환경의 운영 문서에서 관리하고 이 프로젝트 규칙에
  고정하지 않는다.
- AI 결과는 초안 또는 독립 검토 의견이다. 최종 반영자는 기준 문서, 실제 변경 내용과
  검증 결과를 직접 대조한다.

## 3. 변경 안전

- 작업 전 `git status`로 사용자의 기존 변경과 새 파일을 확인하고 보존한다.
- 관련 없는 포맷 변경, 대량 줄바꿈 변환, 에셋 재직렬화를 만들지 않는다.
- 삭제, 강제 리셋, 외부 배포, Git push, 서비스 재시작은 사용자가 명시한 범위에서만
  수행한다.
- 비밀정보, API 키, 토큰, 개인 데이터와 운영 덤프를 코드·문서·로그에 넣지 않는다.
- `Assets/Plugins/`와 다른 외부 패키지는 요청 범위가 아니면 수정하지 않는다.
- Unity 에셋을 추가·이동할 때 `.meta`와 GUID 연결을 보존한다. 씬·프리팹은 가능한
  경우 Unity의 직렬화와 검증 경로를 사용하고, 광범위한 YAML 수정을 피한다.

## 4. Unity C# 코드 규칙

상세 규칙은 `운영/01_코드_작성_규칙.md`가 단일 기준이다. 작업자는 최소한 다음을
지킨다.

- 타입·메서드·프로퍼티는 `PascalCase`, 로컬 변수는 `camelCase`, private 필드는
  `_camelCase`를 사용한다. 기존 파일의 일관된 패턴이 다르면 주변 코드를 우선한다.
- 축약어보다 역할이 드러나는 이름을 사용한다. `bool` 반환 메서드는 가능하면
  `Is`, `Has`, `Can`으로 시작한다.
- enum은 단수형 `PascalCase`로 선언하고 안전한 기본값 `None = 0`을 둔다.
- 저장 키는 고유해야 하며, 저장·로드 계약이나 직렬화 필드 이름을 바꿀 때는 기존
  세이브와 Inspector 참조의 호환성을 검토한다.
- 기획 연동 수치와 규칙은 출처 문서를 확인한다. 미결값을 확정 상수나 저장 계약으로
  만들지 않는다.
- public API, Unity 메시지 메서드, 이벤트 구독·해제, 코루틴과 비동기 흐름의 수명은
  호출부까지 추적한다.
- 존재하지 않는 타입, 필드, 씬 오브젝트, API 계약을 추정하지 않고 실제 소스와
  Inspector 상태를 확인한다.
- 변경은 필요한 최소 범위로 유지하고, 임시 우회보다 원인과 계약을 드러내는
  구현을 우선한다.

## 5. 문서와 공개 범위

- 문서의 첫 화면은 `플레이어가 보는 것 → 선택하는 것 → 결과` 순서로 읽히게 한다.
- 저장 키, 데이터 필드, 원자 규칙과 구현 의사코드는 `구현 참고` 토글로 분리한다.
- 확정 규칙, 제안값, 테스트값, 미결, 백로그를 같은 위계에서 섞지 않는다.
- `TRUE_BASELINE / FILTERED / LEAK / TRUE_VIEW` 인식층을 구분한다. 별도 지정이
  없으면 공개 설명과 기본 에셋은 `FILTERED`를 기준으로 한다.
- 공개 문서에는 구체적인 내부 반전과 진실층 내용을 직접 적지 않는다. 필요한 경우
  내부 문서를 링크하고 공개 문서에는 `숨겨진 진실이 설계되어 있다`는 범위만 남긴다.
- Notion의 `내부 문서` 속성은 분류 표식일 뿐 접근 권한이 아니다. 내부 자료는 실제
  게스트·공유 권한도 별도로 확인한다.
- 외부 작품은 표현 방법을 점검하는 레퍼런스로만 사용한다. 해당 작품의 규칙,
  수치, 세계관을 프로젝트의 확정 근거로 삼지 않는다.

## 6. 기획서·명세서 작성 기준

기획서와 기능 명세서는 배경지식이 없는 새 작업자도 문서만 읽고 동일한 결과를 구현·검증할 수 있을 정도로 쉽고 구체적으로 작성한다. 이 기준은 [「처음 기획서를 쓰는 기획자를 위한 친절한 가이드」](https://brunch.co.kr/@sana-create/14)의 흐름을 프로젝트 규칙에 맞게 정리한 것이다.

문서 유형에 따라 적용되지 않는 항목은 억지로 작성하지 않는다. 생략하거나 `해당 없음`으로 표시하고 이유를 한 문장으로 적는다. 기능·화면·시스템 명세에는 아래 항목을 모두 적용하고, 세계관·스토리·콘텐츠 문서에는 해당 항목만 적용한다.

### 6.1. 작성 순서와 필수 구성

1. **문서 정보**: 문서명, 작성 목적, 대상 독자, 작성일, 최종 수정일과 `초안 / 검토 중 / 확정 / 폐기` 상태를 적는다.
2. **목표와 배경**: 해결할 문제, 필요한 이유와 기대 결과를 적고 성공 기준을 수치나 관찰 가능한 상태로 정의한다.
3. **범위**: 포함 범위와 제외 범위를 각각 적고 후속 작업을 현재 범위와 구분한다.
4. **대상과 전제 조건**: 사용 주체, 시작 위치, 권한·데이터·선행 상태와 관련 용어를 적는다.
5. **사용자 흐름**: 진입부터 완료까지 번호를 붙이고 `누가 → 무엇을 입력·선택하는가 → 시스템 반응 → 다음 상태`를 단계마다 적는다.
6. **화면 설계**: 화면별 목적, 진입·이탈 경로, 표시 요소, 버튼·입력란의 위치와 상태, 안내·오류 문구의 표시 위치를 적는다. 시안만 첨부하지 말고 텍스트로도 설명한다.
7. **기능 정의**: 트리거, 입력값, 처리 규칙, 출력값, 상태 변화, 저장 여부와 후속 이동을 분명히 적는다. `기능 구현`, `적절히 처리` 같은 모호한 표현은 쓰지 않는다.
8. **예외 처리와 오류 메시지**: 잘못된 입력, 중복 요청, 권한 부족, 데이터 없음, 통신 실패, 시간 초과와 재시도의 처리, 정확한 문구, 복구 방법과 로그 필요 여부를 정의한다.
9. **기술적 고려 사항**: 관련 API·데이터·외부 서비스, 성능·보안·호환성 제약과 기존 저장 데이터 또는 다른 기능에 미치는 영향을 적는다. 미확정 구현 방식은 확정 요구사항과 분리한다.
10. **완료 조건과 검증 방법**: 정상·실패·경계 조건별 재현 절차와 기대 결과를 적어 문서만 보고 통과 여부를 판단할 수 있게 한다.
11. **미결 사항과 관련 문서**: 결정할 질문, 담당자, 결정 시점과 근거·연결 문서를 적는다.
12. **변경 이력**: 날짜, 변경자, 변경 요약과 이유를 기록한다.

### 6.2. 문장과 근거 작성 원칙

- 한 문장에는 하나의 규칙만 쓰고 주어, 조건, 행동과 결과를 생략하지 않는다.
- `빠르게`, `적당히`, `필요한 경우`, `정상적으로`는 측정 가능한 시간·횟수·조건·상태로 바꾼다.
- 버튼명, 화면명, 필드명, 상태명과 오류 문구는 실제 표시 문자열 또는 공통 용어와 일치시킨다.
- 입력값에는 형식, 길이, 허용 범위, 필수 여부와 기본값을, 출력값에는 표시 위치와 형식을 적는다.
- 분기마다 진입 조건과 결과를 적고 핵심 규칙을 이미지 안에만 두지 않는다.
- 확정, 제안, 테스트, 추정과 미결 상태를 명시한다. 미결 항목에는 담당자와 결정 시점 또는 필요한 질문을 적는다.
- 수치, 규칙, 상태, 용어와 기존 기능에는 가능한 경우 근거 문서나 확인한 구현 위치를 적는다. 근거가 없으면 사실처럼 쓰지 않고 `추정`, `제안` 또는 `미결`로 표시한다.
- 기준 문서가 충돌하면 임의로 선택하지 않는다. 충돌 내용, 각 출처와 영향을 기록하고 미결 사항으로 올린다.
- 피드백으로 규칙이 바뀌면 관련 화면·예외·검증 항목도 함께 고친다.

### 6.3. 쉬운 문장 쓰기

기획서는 정확해야 하지만, 먼저 읽기 쉬워야 한다. 팀 동료가 한 번 읽고
이해하지 못하면 그 문서는 목적을 이루지 못한 것이다.

- 일상 대화에서 쓰는 쉬운 말을 먼저 고른다. 같은 뜻이면 짧고 쉬운 단어를 쓴다.
- 딱딱한 문어체 한자어는 쉬운 말로 바꾼다. 예시는 다음과 같다.
  - `명시한다` → `분명히 적는다`
  - `기재한다` → `적는다`
  - `부여한다` → `붙인다`
  - `수행한다` → `한다`, `진행한다`
  - `판정한다` → `판단한다`, `정한다`
  - `대조한다` → `비교해서 확인한다`
  - `갱신한다` → `업데이트한다`
  - `산출물` → `결과물`
  - `상이하다` → `다르다`
  - `용이하다` → `쉽다`
  - `해당` → `그`, `이` (지시 대상이 분명할 때)
  - `별도로` → `따로`
  - `필히` → `반드시`
  - `상기` / `하기` → `위의` / `아래의`
- 한 문장은 60자를 넘기지 않는 것을 기준으로 한다. 길어지면 두 문장으로 나눈다.
- 피동형보다 능동형으로 쓴다. `표시된다`보다 `시스템이 표시한다`로 쓴다.
- 이중 부정을 쓰지 않는다. `없지 않다`가 아니라 `있다`로 쓴다.
- 한 단락은 3~4문장을 넘기지 않는다. 내용이 많으면 목록이나 표로 바꾼다.
- 전문 용어와 프로젝트 용어는 처음 나올 때 한 문장으로 뜻을 풀어 쓰거나
  `용어 사전` 링크를 건다. 팀에 정착된 용어는 임의로 다른 말로 바꾸지 않는다.
- 영어 약어는 처음 나올 때 원어나 뜻을 함께 적는다. 예: `QA(품질 검증)`.
- 정확성과 쉬운 표현이 부딪히면 정확성을 지키되, 어려운 표현 뒤에 쉬운
  설명이나 예시를 한 문장 덧붙인다.
- 규칙을 적은 뒤에는 가능하면 구체적인 예시를 하나 붙인다. 예시가 있으면
  긴 설명보다 빨리 이해된다.

### 6.4. 요구사항 추적과 게임 상태

- 여러 화면이나 시스템에 영향을 주는 요구사항에는 `REQ-기능명-001`, `ERR-기능명-001`, `QA-기능명-001` 형식의 고유 식별자를 붙인다.
- 식별자는 의미가 유지되는 동안 바꾸지 않고, 삭제한 식별자를 재사용하지 않는다.
- 게임 상태가 바뀌는 기능은 변경 전 상태, 발생 조건, 변경 후 상태, 저장 시점과 다시 들어왔을 때 복원되는 상태를 분명히 적는다.
- 일회성·반복 가능 여부와 씬 이동·게임 종료·불러오기 이후 유지 여부를 기록한다.

### 6.5. 검증 시나리오 형식

검증 조건은 가능한 경우 `Given–When–Then`으로 작성한다.

- **Given**: 검증 시작 전 상태와 데이터
- **When**: 플레이어 행동 또는 발생 이벤트
- **Then**: 화면, 데이터, 저장 상태와 후속 흐름의 기대 결과

각 시나리오는 한 번에 재현할 수 있어야 한다. 여러 결과는 `Then` 아래에 목록으로 분리하고 정상·실패·경계 조건을 각각 작성한다.

### 6.6. 별도 양식이 없을 때의 기본 문서 구조

1. 문서 개요
2. 목표와 배경
3. 포함 범위와 제외 범위
4. 용어와 전제 조건
5. 사용자 또는 플레이어 흐름
6. 화면 및 기능 명세
7. 데이터·게임 상태·저장 변화
8. 예외 처리와 오류 메시지
9. 기술적 고려 사항
10. 완료 조건과 검증 시나리오
11. 미결 사항
12. 변경 이력과 관련 문서

### 6.7. 자체 점검

- 처음 보는 작업자가 목적, 대상 독자와 포함·제외 범위를 설명할 수 있는가?
- 추가 구두 설명 없이 시작 조건부터 완료 상태까지 재현할 수 있는가?
- 입력, 시스템 반응, 화면 이동, 데이터·저장·게임 상태 변화가 연결되어 있는가?
- 실패·경계 상황의 처리, 실제 사용자 메시지와 복구 방법이 정의되어 있는가?
- 사실의 근거와 확정·제안·추정·미결 상태가 구분되어 있는가?
- 요구사항, 구현 항목과 검증 시나리오를 식별자로 추적할 수 있는가?
- 구현자와 검수자가 같은 완료 기준으로 통과 여부를 판단할 수 있는가?
- 소리 내어 읽었을 때 걸리는 문장, 어려운 한자어, 60자를 넘는 문장이 없는가?
- 전문 용어와 약어에 설명이나 용어 사전 링크가 있는가?

하나라도 답할 수 없으면 해당 항목을 보완하거나 미결로 명시한 뒤 공유한다.

## 7. 검증과 완료 기준

- 아래 필수 검증과 변경에 직접 필요한 검증을 수행한다. 통과한 검증은 새 변경,
  실패 또는 미해결 위험이 있을 때만 확대하거나 반복한다.
- 문서 변경은 관련 페이지를 다시 조회해 새 문구, 링크, 토글 계층, 확정·미결 상태를
  확인한다.
- 기존 명세를 수정할 때는 연결된 화면, 흐름, 데이터, 저장 계약, 오류 처리, 검증
  조건과 관련 문서를 함께 검색한다. 다른 문서에 이전 규칙이 남아 있지 않은지 확인한다.
- C# 변경은 최소한 Unity 컴파일 오류가 없는지 확인하고 직접 관련 흐름을 검증한다.
- 씬·프리팹·에셋 변경은 대상 씬 재로드, Inspector 연결, 플레이 모드 또는 관련
  자동 테스트 중 가능한 경로로 확인한다.
- 테스트하지 못한 항목과 환경 제한은 검증 완료로 표현하지 않는다.
- 최종 보고에는 변경한 파일·Notion 페이지, 확인한 테스트, 남은 미결과 운영 검증을
  간결하게 포함한다.
