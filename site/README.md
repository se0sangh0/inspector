# INSPECTOR 웹사이트

랜딩 페이지와 GitHub Pages 배포 파일은 이 저장소 안의 `site/`에서 관리합니다.
별도 웹 저장소를 만들지 않습니다.

- `preview/`: GitHub Pages에 공개하는 개발 시안
- `preview/assets/`: 기존 웹 이미지 6종, 공유용 합성 이미지와 사건 삽화
- `preview/assets/animations/`: 8프레임 애니메이션의 웹용 시트 12종과 첫 프레임 12종
- `animation-assets.json`: 웹용 파생본의 원본 경로·해시·크기 기록
- 공개 주소: [https://davi-dev.uk/](https://davi-dev.uk/)
- 배포 방식: GitHub Actions가 `preview/`를 GitHub Pages 아티팩트로 배포
- 워크플로: [`.github/workflows/deploy-pages.yml`](../.github/workflows/deploy-pages.yml)
- 개선 작업 기준: [랜딩페이지 개선 작업지시서](01_랜딩페이지_개선_작업지시서.md) · 디자인·모델 배정·검수·사용량 초과 후 Luna 전환 규칙

현재 [`preview/index.html`](preview/index.html)은 한국어·영어·중국어 번체를
한 페이지에서 바꾸는 공개 개발 시안입니다. 영상 자리는 각 언어의 설명용 문구로
남아 있습니다.

브랜드 정본은 `art/brand/`, 그래픽 검토 원본은
`art/production/graphics-remake/`에 유지합니다. 사이트에서는 정본을 직접 수정하지
않고 웹용 파생본을 `site/preview/assets/`에 만듭니다.

`main`의 `site/preview/**` 또는 배포 워크플로가 바뀌면 자동 배포합니다. GitHub Actions의
`Run workflow`로 수동 배포할 수도 있습니다.

현재 페이지는 개발 중 콘셉트 비주얼을 사용하는 공개 시안입니다. 정식 배포본으로
전환하기 전 승인된 리소스, 공유 이미지와 동영상 파일을 다시 확인합니다.

## 2026-09-20 전시 이후 소식 · 스토리 소개 반영

- 2026-09-20 사용자가 제공한 선정·수상 이력을 한국어·영어·번체중국어에 반영했습니다.
  대구게임인재육성사업 예비 창업팀 선정, 2026 대만 이노테크 전시회 동상·특별상,
  대만 이노테크 전시회에서 함께 진행된 2026 태국 발명전시회 특별상입니다.
- 사업 선정과 수상은 구분했습니다. 대구 사업의 선정 연도와 특별상 수여기관명은
  제공되지 않아 임의로 추가하지 않았습니다. 사용자 입력의 `20206`은 `2026`으로 해석했습니다.
  외국어 행사명은 사용자 제공 명칭의 설명형 번역입니다.
- Steam 출시 예정 상태와 **스토어 페이지 2026년 12월~2027년 2월 개설 예정**을
  첫 화면과 하단 정보에 표시했습니다. 게임 출시일로 표현하거나 미개설 스토어 링크를 만들지 않았습니다.
- 스토리 소개에 한국·일본·중국·대만 등지의 민간 설화 모티프를 추가했습니다.
  개별 설화·등장인물·구현된 콘텐츠 수는 새로 확정하지 않았습니다.
- 기존 게임 코드와 아트는 수정하지 않았습니다. 이번 반영은 로컬 수정이며 공개 배포는 별도입니다.
- 검증: 1440px·360px × 세 언어 6조합에서 문구와 가로 넘침을 확인했습니다.
  스팀 일정·수상 행·스토리 설명의 넘침은 없고 브라우저 오류·경고도 0건입니다.
  한국어 모바일 첫 화면·수상·설화, 영문 데스크톱 수상, 번체 모바일 설화를 화면으로 확인했습니다.
  세 언어 번역 키는 각각 90개로 일치하며 HTML에서 사용하는 키가 모두 정의돼 있습니다.
  JS 구문, 기존 애니메이션 코드 구간 보존, `git diff --check`를 확인했고
  `node --test site/tests/animation-player.test.cjs`는 10개 모두 통과했습니다.

## 2026-09-12 이벤트 1 삽화 · 랜딩 사용 승인 및 로컬 반영

- 사용자가 제작된 EVT-01 이미지의 랜딩 적용을 승인했습니다. 이전 핸드오프의 이벤트 삽화 제외 항목은 이번 요청으로 랜딩 범위에서 해제됐습니다. Unity 연결이나 다른 아트의 승인 상태는 바꾸지 않았습니다.
- `preview/index.html`의 사건 영역에 `preview/assets/event-cold-camp.webp`를 연결했습니다. 데스크톱은 설명·그림을 좌우로, 900px 이하에서는 위아래로 배치합니다. 그림은 자르지 않고 전체 가로 비율로 표시하며 지연 로딩과 고유 크기를 지정했습니다.
- 세 언어의 예정 안내를 실제 삽화 설명과 캡션으로 바꿨습니다. 영어 제목 `A Cold Campsite`는 기존 게임 번역을 따릅니다. 더 이상 쓰지 않는 `eventArtPlaceholder` 키를 제거해 세 언어의 키 개수는 각각 75개입니다.
- 입력은 `art/production/graphics-remake/wave-a/event-illustrations/2026-09-09/EVT-01_Cold_Camp_v1.png`입니다. 1672×941 무손실 WebP로 변환했고, 원본과 디코딩한 RGB 픽셀이 같음을 확인했습니다. 웹 파일은 1,926,384바이트이며 원본 PNG는 그대로입니다.
- 원본 SHA-256: `faedf2fc237cd09decb14744e80db130ae768a346c29b00668cad04f9d32c265`. 웹 파일 SHA-256: `17c3fe04a6cb4e818550f624160aabd42e609aec6572238f37cf2f06be46e57a`.
- 1440·390·360px × 한국어·영어·번체중국어 9조합에서 이미지 로딩, 원본 비율, 영역 밖 넘침과 번역 문구를 확인했습니다. 실제 데스크톱·360px 화면에서 화덕·약초·도구가 전체 구도 안에 보이며 브라우저 오류는 없었습니다. 기존 애니메이션 검사 10개와 JS·번역 키·변경 공백 검사를 통과했습니다.
- 이번 반영은 로컬 수정본입니다. 커밋·push·공개 배포는 수행하지 않았습니다.

## 2026-09-12 랜딩 개선 1차 · 로컬 구현 완료

기준은 [Claude 구현 스펙](../docs/handoff/랜딩페이지_개선_1차.md)과
[실행 지시](../docs/handoff/랜딩페이지_개선_1차_실행지시.md)입니다.
REQ-LAND-001~008을 반영했습니다. 이번 변경은 커밋·push·배포하지 않았습니다.

| 변경 파일 | 실제 반영 내용 |
| --- | --- |
| `preview/index.html` | 저장된 선택 → 브라우저 언어 → 번체중국어 순서로 시작 언어 결정, 저장 실패 처리, 첫 적용은 동기 실행. 한글·번체 라벨 자간, Noto Sans TC CDN 연결, OG·Twitter 메타, 문의 링크, 미사용 번역 키 8개 제거, 거두는 자 이미지 지연 로딩·크기 지정 |
| `preview/favicon.svg` | 루트 색을 `#c39a52`로 지정. 세 경로 데이터는 보존 |
| `preview/assets/og-cover.webp` | 기존 첫 화면 구도를 1200×630으로 합성. 60,624바이트, quality 85 |
| `tools/build-og-cover.py` | 지정된 웹용 입력 5개의 해시를 확인하고 같은 공유 이미지를 만드는 Pillow 스크립트 |
| `preview/assets/action-caster.webp`, `action-goblin.webp`, `companion-offender.webp`, `companion-priest.webp` | 사용되지 않는 웹 파생본 4개 삭제. 실제 합계 127,000바이트 |
| `README.md` | 구현·검사·전송량과 남은 외부 확인 기록 |

Space Grotesk, JetBrains Mono, Noto Sans TC는 Google Fonts CDN으로 연결하며 모두 OFL입니다.
폰트 스택, 정적 번체 메타, 애니메이션 제어 구간과 검사 파일, 아트 원본·Unity·HANDOFF는 보존했습니다.

### 확인 결과

- 1440·390·360px × 번체중국어·한국어·영어 9조합에서 가로 넘침, 버튼·푸터 겹침이 없었습니다. 문의 링크는 44×44px 이상이며 키보드 포커스가 보입니다. 가장 밝은 기본 배경색을 기준으로 계산한 링크 명암비도 5.83:1입니다.
- 한글·번체 라벨 자간은 0.12~0.13px, 영어는 기존 1.68~2.6px를 유지합니다. 흐름 번호는 세 언어 모두 2.4px입니다.
- 실제 소스의 언어 함수 16개 사례와 저장 쓰기 실패 검사를 통과했습니다. 별도 임시 계측 사본의 실제 브라우저에서도 지정된 로캘 6개와 저장소 읽기·쓰기 예외를 확인했습니다. 한국어 브라우저에서 선택한 번체중국어는 새로고침과 탭 재개 뒤에도 유지됐습니다.
- Chrome 152의 `3G` 프로필에서 첫 콘텐츠 페인트는 9,244ms였고, 첫 제목·문서 언어·선택기 모두 한국어였습니다. 메뉴에는 `Slow 3G` 대신 `3G`가 있어 해당 프로필을 사용했습니다. 계측 코드는 배포 파일에 넣지 않았습니다.
- DevTools의 Rendered Fonts에서 `查看玩法` 네 글자가 네트워크의 Noto Sans TC, 굵기 500으로 그려짐을 확인했습니다. 폰트 두 도메인을 차단한 360px 화면에서도 세 언어와 조작이 유지됐습니다. 검사 후 차단·속도·캐시 설정을 복원했습니다.
- 동작 줄이기 에뮬레이션에서 reveal 요소 22개가 모두 보였고, 언어 전환과 직접 선택한 애니메이션 재생이 작동했습니다. 동료 4명·적 2종 이미지, 세 언어 재생 라벨, 조사대 Home·방향키와 포커스도 확인했습니다. 최종 인앱 브라우저의 오류 기록은 0건입니다.
- `node --test site/tests/animation-player.test.cjs`는 10개 통과했습니다. 기존 검사 파일과 잘라내는 애니메이션 코드 구간은 바이트 그대로입니다. 번역 키는 세 언어 각각 76개이며 이름 집합도 같습니다. 전체 JS 구문과 변경 공백 검사도 통과했습니다.
- 공유 이미지 입력 5개와 애니메이션 24개는 해시가 그대로입니다. 공유 이미지는 배경·유닛을 확대하거나 자르지 않았고, 지정 좌표·겹침 순서로 합성했습니다. 파비콘은 밝고 어두운 배경에서 16·32px로 확인했습니다.

### 폰트 전송량 실측

Chrome DevTools에서 캐시를 끄고 기록한 값입니다. 언어 전환 전마다 네트워크 기록을 비웠습니다.

| 조건 | 실제 파일 수 | 리소스 본문 크기 | 네트워크 전송 크기 |
| --- | ---: | ---: | ---: |
| 번체중국어 CSS | 1 | 압축 100,608 B / 해제 372,303 B | 약 100.7 KB |
| 번체중국어 폰트 전체 | 18 | 1,080,252 B | 1,080,927 B |
| 위 항목 중 Noto Sans TC | 16 | 1,026,592 B | 약 1,027.2 KB |
| 한국어 전환 시 추가 | 3 | 154,812 B | 154,909 B |
| 이후 영어 전환 시 추가 | 0 | 0 B | 0 B |

폰트 본문 합계는 스펙 추정치 1,357,284 B보다 20.4% 작았습니다.
추정은 원문의 문자 집계이고 실측은 브라우저가 실제로 요청한 조각 집계입니다.
사용되지 않는 키를 지운 뒤 번체 사전의 고유 한자는 274자에서 250자로 줄었으며,
대체 텍스트·ARIA 문구·비활성 문구의 문자 집계를 실제 폰트 요청과 동일시하지 않았습니다.
페이지 하단까지 확인한 재측정에서도 18개·1,080,252 B였습니다.
승인된 CDN·스택 정책은 유지했습니다.

위 표의 `한국어 전환 시 추가 3개`는 번체중국어를 이미 받은 뒤의 증가분입니다.
**처음부터 한국어나 영어로 여는 방문자의 비용은 이 값과 다릅니다.**
2026-09-12에 각 언어에서 실제로 필요한 조각을 하나씩 내려받아 다시 쟀습니다.

| 첫 방문 언어 | 필요한 TC 조각 | 합계 | 한글만 담당하는 조각 | 낭비 비율 |
| --- | ---: | ---: | ---: | ---: |
| 번체중국어 | 16 | 1,026,592 B | 0개 · 0 B | 0.0% |
| 한국어 | 8 | 453,784 B | 5개 · 308,932 B | 68.1% |
| 영어 | 5 | 298,972 B | 2개 · 154,120 B | 51.5% |

번체중국어 값 1,026,592 B는 위 DevTools 측정과 정확히 같습니다.

한글 전용 조각을 받는 이유는 두 가지입니다.
첫째, Noto Sans TC의 `unicode-range`에 한글 음절 49자가 선언돼 있습니다.
둘째, 언어 선택기의 `한국어`·`繁中` 옵션 문구는 어떤 언어에서도 DOM에 남습니다.
그래서 영어로 여는 방문자도 `한`·`국`·`어` 때문에 조각 2개를 받습니다.

**이 조각들은 화면에 아무것도 그리지 않습니다.** 선언된 범위 안에 글리프가 없어
브라우저가 글자마다 다음 폰트로 넘기기 때문입니다. 렌더링 폭 비교로 확인했습니다
(한자 `怪`는 폭 40, 한글 `이`·`국`은 폴백과 같은 34.6).

줄이려면 `Noto Sans TC`를 `:lang(zh-Hant)` 범위에만 적용하면 됩니다.
한국어·영어 화면에서 이 폰트는 어차피 아무것도 그리지 않으므로 보이는 결과는
달라지지 않습니다. 이번 승인 범위가 아니라 적용하지 않았습니다.

### 성능과 스펙에서 보완한 부분

- Lighthouse 13.4.1 모바일 검사에서 변경 전 CLS는 0이었습니다. 첫 수정본은 언어 선택창 너비 변화로 0.001이 나와 선택창 폭을 112px로 고정했습니다. 재검사 CLS는 0입니다. 이 한 줄은 폰트 교체 시 레이아웃 이동을 막기 위한 보완입니다.
- 같은 검사의 변경 전 FCP/LCP는 3.0/3.3초, 최종 수정본은 8.1/8.7초였습니다. 최종 접근성 점수는 100입니다. 사용자 Chrome의 확장 프로그램·네트워크·기기 부하가 포함된 단일 실행값이며, 반복 측정한 성능 보장은 아닙니다. 큰 CDN 스타일시트와 폰트의 로딩 지연은 남아 있습니다. 자체 호스팅·글자 조각 생성·요청 굵기 축소는 승인 범위 밖이라 적용하지 않았습니다.
- 실행 지시서의 키 개수 75개는 문의 키 추가분을 빠뜨린 값입니다. 본문 스펙의 `83 - 8 + 1 = 76`을 따랐습니다. 삭제 자산의 본문 합계 126,999 B도 실제 파일 합계 127,000 B로 기록했습니다.
- Codex 앱 세션에서 직접 구현·검수했고 별도의 `codex exec`는 실행하지 않았습니다. 따라서 CLI 경로·버전·종료 코드를 구현 성공 근거로 쓰지 않았습니다. OG 합성과 HTML 독립 검토는 네이티브 GPT-5.6 Luna 작업자가 맡았습니다. 셸 검사의 정상 종료와 실제 diff를 대조했습니다.

### EVT-01 사건 삽화 연결

- 사건 구간의 `준비 중` 자리를 승인된 EVT-01 `식어 있는 야영지` 삽화로 바꿨습니다.
  사용자가 2026-09-12에 시각 검토하고 공개를 승인했으며,
  `art/review/2026-09-09-environments/manifest.json`의 `approval`을
  `approved_2026-09-12`로 갱신했습니다. Unity 연결은 그대로 `false`입니다.
- 원본이 1672×941 가로형이라 사건 구간 배치를 세로 4:5에서 가로형으로 바꿨습니다.
  원본을 자르거나 확대하지 않았습니다.
- 웹 파생본 `preview/assets/event-cold-camp.webp`은 1400×788 · 223,122 B입니다.
  원본 PNG에서 한 번만 변환했으며 이미 압축된 파일을 다시 압축하지 않았습니다.
  재생성 절차는 `tools/build-event-art.py`이고 원본 SHA-256을 확인한 뒤에만 만듭니다.
- 첫 파생본은 원본 해상도 그대로라 1,926,384 B였습니다. 화면 표시 폭이 최대 678px인데
  파일이 1672px이어서 1400px·품질 85로 다시 만들었습니다. 1,703,262 B를 줄였습니다.
- 1440·360px × 세 언어에서 가로 넘침과 겹침이 없고 콘솔 오류도 없습니다.
  삽화는 `loading="lazy"`이며 고유 크기를 지정해 로드 시 레이아웃이 밀리지 않습니다.
- 준비 중 표시는 30초 영상 자리에만 남았습니다. 영상은 2026-09-15에 연결할 예정입니다.

### 남은 외부 확인

- 브라우저 보안 정책이 `mailto:` 실행을 차단해 실제 메일 앱 실행은 확인하지 못했습니다. 주소·라벨·클릭 영역·포커스는 확인했습니다.
- 실제 SNS 공유 카드와 WebP 지원 여부는 배포 뒤에 확인합니다. 이번에는 로컬 메타와 파일만 검수했고 외부 대화·게시물에 링크를 보내지 않았습니다.

## 2026-09-11 공개 배포 완료

- 사용자의 공개·배포 요청에 따라 [davi-dev.uk](https://davi-dev.uk/)에 8프레임 애니메이션을 공개했습니다.
- 배포 소스는 `6743558e497f4a59fcf7d9149daeea48208985c2`입니다. [GitHub Pages 배포](https://github.com/se0sangh0/inspector/actions/runs/34537992934)가 성공했습니다.
- 공개 주소의 HTML 1개와 웹용 애니메이션 파일 24개를 내려받아 검증본과 바이트 해시가 같음을 확인했습니다. 실제 공개 페이지에서 한국어 전환과 애니메이션 재생도 확인했습니다.
- 공개 범위는 랜딩 화면과 웹용 파생본입니다. 제작 원본·나머지 아트 검토 묶음과 Unity 연결 상태는 이번 배포에 포함하지 않았습니다.

## 2026-09-10 8프레임 애니메이션 · 로컬 검수 완료

- 사용자의 랜딩 반영 요청에 따라 동료 4종과 적 2종의 대기·기본 동작 12종을 연결했습니다. 행동 예시 2곳, 조사대 4곳, 적 소개 2곳에서 볼 수 있습니다.
- 첫 화면에서는 정지 포스터를 표시합니다. `대기 / 기본 동작`으로 고른 뒤 `재생`을 누르면 필요한 시트만 불러옵니다. 일시정지, 동료 전환·화면 이탈 시 정지, 로딩·재시도 문구를 세 언어로 제공합니다.
- 웹용 파일은 정리된 PNG에서 만든 무손실 WebP입니다. 1536×1024 시트의 4×2 배치, 프레임당 384×512, 투명도와 RGBA 픽셀 일치를 확인했습니다. 시트 12종은 총 7,543,998바이트, 포스터 12종은 총 927,430바이트입니다.
- 재생 주기는 대기 1.6초, 기본 동작 0.9초인 웹 미리보기 값입니다. 게임의 전투 속도나 효과 발생 시점을 확정한 값은 아닙니다.
- 실제 브라우저에서 1440·390·360px × 한국어·영어·번체중국어 9조합을 확인했습니다. 그림 비율, 포스터와 시트 크기, 가로 넘침, 컨트롤·캡션 겹침과 44px 버튼 영역 검사를 통과했습니다. 12개 동작의 실제 로딩·재생, 프레임 진행, 화면 이탈 시 정지, 동료 선택과 키보드 포커스도 확인했습니다. 브라우저 오류 기록은 없었습니다.
- `node --test site/tests/animation-player.test.cjs`의 10개 검사를 통과했습니다. 지연 응답·모션 전환 경합, 이미지 실패·재시도, 문서 숨김과 동작 줄이기 설정 변경은 이 단위 검사에서 확인했습니다. 이번 검수에서 운영체제의 동작 줄이기 설정을 직접 변경하지는 않았습니다.
- Luna 구현·독립 검토 후 PM이 실제 화면에서 남아 있던 모바일 고정 높이와 이미지 스타일 충돌을 수정하고 최종 검수했습니다. 원본 아트와 Unity 파일은 보존했습니다.
- Git push와 공개 배포는 수행하지 않았습니다. 기존 사건 삽화·영상의 준비 상태는 유지합니다. Unity 연결·인게임 검증과 아트 최종 승인은 별도입니다.

## 2026-09-08 랜딩 개선 · 로컬 검수 완료

- 먹색·황동색을 유지하고 첫 화면의 제목·장르·상태와 카드 흐름의 위계를 정리했습니다.
- 주 버튼은 `플레이 방식 보기 / See how it plays / 查看玩法`이며 카드 흐름으로 이동합니다.
- 조사대는 인물과 설명을 나눠 배치하고 역할 직접 선택 버튼을 추가했습니다. 이전·다음과 키보드 조작도 유지합니다.
- 거두는 자는 세로 원본 비율로 표시하고, 준비 중인 사건 삽화·영상 영역은 간결하게 정리했습니다.
- 이미지 10종은 기존 HTML 내장본과 바이트 해시가 같습니다. 새 그림이나 게임 규칙을 추가하지 않았습니다.
- 실제 브라우저에서 1440·390·360px × 한국어·영어·번체중국어 9조합을 검증했습니다. 가로 넘침, 제목 단어 잘림, 버튼 이동, 역할 선택·키보드 포커스, 이미지 로딩과 JS 오류 검사를 통과했습니다.
- 일반 동작과 동작 줄이기 환경에서 콘텐츠 표시를 확인했습니다. 이미지 비율과 주요 구간은 실제 화면으로 대조했습니다.
- Opus 5의 `max` 호출이 수정본 작성 후 세션 한도로 종료돼 이후 하위 작업을 Luna로 전환했습니다. Gemini의 사전 시작 검토 결과와 Luna 독립 검토를 대조하고, Luna가 역할 선택 버튼의 키보드 포커스를 마무리했습니다.
- 공개 사이트 배포와 Git push는 아직 수행하지 않았습니다. 위 결과는 로컬 수정본 검증입니다.
